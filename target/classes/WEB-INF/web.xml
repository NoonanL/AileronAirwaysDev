<?xml version="1.0" encoding="UTF-8"?>
<web-app id="aileronAirways" version="2.4" xmlns="http://java.sun.com/xml/ns/j2ee" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://java.sun.com/xml/ns/j2ee http://java.sun.com/xml/ns/j2ee/web-app_2_4.xsd">

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>IndexServlet</servlet-name>
        <servlet-class>application.servlet.IndexServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>IndexServlet</servlet-name>
        <url-pattern>/indexServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>TimelineRegisterServlet</servlet-name>
        <servlet-class>application.servlet.TimelineRegisterServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>TimelineRegisterServlet</servlet-name>
        <url-pattern>/timelineRegisterServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>EventDetailsServlet</servlet-name>
        <servlet-class>application.servlet.EventDetailsServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>EventDetailsServlet</servlet-name>
        <url-pattern>/eventDetailsServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>LinkedEventsServlet</servlet-name>
        <servlet-class>application.servlet.LinkedEventsServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>LinkedEventsServlet</servlet-name>
        <url-pattern>/linkedEventsServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>SearchTimelinesServlet</servlet-name>
        <servlet-class>application.servlet.SearchTimelinesServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>SearchTimelinesServlet</servlet-name>
        <url-pattern>/searchEventsServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>AddEventServlet</servlet-name>
        <servlet-class>application.servlet.AddEventServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>AddEventServlet</servlet-name>
        <url-pattern>/addEventServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>AttachmentServlet</servlet-name>
        <servlet-class>application.servlet.AttachmentServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>AttachmentServlet</servlet-name>
        <url-pattern>/attachmentServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>DeleteAttachmentServlet</servlet-name>
        <servlet-class>application.servlet.DeleteAttachmentServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>DeleteAttachmentServlet</servlet-name>
        <url-pattern>/deleteAttachmentServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>DeleteTimelineServlet</servlet-name>
        <servlet-class>application.servlet.DeleteTimelineServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>DeleteTimelineServlet</servlet-name>
        <url-pattern>/deleteTimelineServlet</url-pattern>
    </servlet-mapping>


    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>SelectTimelineServlet</servlet-name>
        <servlet-class>application.servlet.SelectTimelineServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>SelectTimelineServlet</servlet-name>
        <url-pattern>/selectTimelineServlet</url-pattern>
    </servlet-mapping>

    <!--Declare name of servlet and its location (application.servlet.YourNewServlet-->
    <servlet>
        <servlet-name>SelectEventServlet</servlet-name>
        <servlet-class>application.servlet.SelectEventServlet</servlet-class>
    </servlet>

    <!--Map the servlet to a url pattern - this url pattern is what you will call in submit buttons etc. -->
    <servlet-mapping>
        <servlet-name>SelectEventServlet</servlet-name>
        <url-pattern>/selectEventServlet</url-pattern>
    </servlet-mapping>


</web-app>